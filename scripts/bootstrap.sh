#!/usr/bin/env sh
#
# scripts/bootstrap.sh
#
# Purpose:
# Setup convenience script for local development.
#
# What code should live here:
# - optional checks (docker installed, ports free)
# - `docker compose build` / `docker compose up` if desired
# - initial folder/sample-data population (if you choose to add it later)
#
# Placeholder only.

echo "Bootstrap placeholder: implement checks/setup as needed."

