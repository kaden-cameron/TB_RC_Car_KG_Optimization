// data_team/graph/cypher/constraints.cypher
//
// Purpose:
Define Neo4j constraints (e.g. uniqueness) for graph data integrity.
//
// What code should live here:
// - `CREATE CONSTRAINT ...` statements
// - kept separate from data-loading queries so the graph schema stays clear.

