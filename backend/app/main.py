"""
backend/app/main.py

Purpose:
Create the FastAPI app, register API routers, and expose the HTTP server.

What code should live here:
- FastAPI app creation
- router registration (health + chat)
- application startup/shutdown hooks (later, when graph connections exist)

This file is currently a placeholder to establish the repo structure.
"""

