"""
backend/app/core/logging_config.py

Purpose:
Configure consistent backend logging (format, levels, sinks).

What code should live here:
- logging formatter configuration
- log level selection via environment variables
- structured logging setup (later) if desired
"""

