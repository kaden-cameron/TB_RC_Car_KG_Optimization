"""
backend/app/core/config.py

Purpose:
Centralized configuration loading for the FastAPI backend.

What code should live here:
- read environment variables (Neo4j URI, credentials, ports, toggles)
- validate/configure defaults
- expose a single config object for the rest of the backend
"""

