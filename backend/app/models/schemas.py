"""
backend/app/models/schemas.py

Purpose:
Pydantic request/response schemas for the chat API.

What code should live here:
- `ChatRequest` schema (what the frontend sends)
- `ChatResponse` schema (what the backend returns)
- `HealthResponse` schema (what `/health` returns)

This is currently a placeholder scaffold.
"""

