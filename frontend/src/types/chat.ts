/**
 * frontend/src/types/chat.ts
 *
 * Purpose:
 * TypeScript types shared between frontend chat components and API layer.
 *
 * What code should live here:
 * - `ChatRequest` / `ChatResponse` type shapes matching backend contracts
 */

export type ChatRequest = unknown;
export type ChatResponse = unknown;

