/**
 * frontend/src/components/ChatLayout.tsx
 *
 * Purpose:
 * Layout wrapper for the chatbot UI (message list + input + header).
 *
 * What code should live here:
 * - page structure/styling hooks
 * - assembling child components into one layout
 */

export default {};

