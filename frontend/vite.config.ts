/**
 * frontend/vite.config.ts
 *
 * Purpose: Vite dev/build configuration for the chatbot UI.
 *
 * What code should live here:
 * - Vite plugins (React)
 * - server/host settings
 * - optional proxy rules to backend (so `/api/...` calls work in dev)
 *
 * Placeholder only.
 */

export default {};

